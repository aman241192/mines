import React from "react";

const CutomText = ({ title }) => {
  return (
    <>
      <label className="text-lightGray text-[14px] font-semibold block">
        {title}
      </label>
    </>
  );
};

export default CutomText;
